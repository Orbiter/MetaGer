update:
  before_script:
    # Abhängigkeiten überprüfen
    - which virtualbox
    - which vagrant
    - which composer
    - which git
    - which php
    # Pfad zum neu geklonten Repo
    - path=`pwd`
    - cd ~/
    - $path/deploy/checkServerStatus.sh "$path"
  script:
    - cd ~/MetaGer
    - git pull "$path"
  only:
    - master